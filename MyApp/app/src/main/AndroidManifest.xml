<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.tuankiet.myapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <application
        android:name="com.orm.SugarApp"
        android:allowBackup="false"
        android:icon="@drawable/ic_michat"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DialogListActivity" />
        <activity android:name=".MessageListActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value="DialogListActivity" />
        </activity>
        <activity
            android:name=".TabActivity"
            android:label="@string/title_activity_tab"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".voicecall.MakeCallActivity"
            android:label="Make Call"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".voicecall.ReceiveCallActivity"
            android:label="Receive Call"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".voicecall.MainActivity2"
            android:label="Call"
            android:theme="@style/AppTheme.NoActionBar" />

        <service
            android:name=".service.MainService"
            android:enabled="true"
            android:exported="true" />

        <activity android:name=".FriendListActivity" />

        <meta-data
            android:name="DATABASE"
            android:value="chat_sugar" />
        <meta-data
            android:name="VERSION"
            android:value="12" />
        <meta-data
            android:name="QUERY_LOG"
            android:value="true" />
        <meta-data
            android:name="DOMAIN_PACKAGE_NAME"
            android:value="com.example.tuankiet.myapp" />

        <activity android:name=".AddGroupActivity" />
        <activity android:name=".SignupActivity" />
    </application>

</manifest>